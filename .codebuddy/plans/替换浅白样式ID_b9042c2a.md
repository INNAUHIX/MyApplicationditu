---
name: 替换浅白样式ID
overview: 将「浅白」地图样式的 styleId 从 1992174833714153216 替换为新 ID 1992216939216384896
todos:
  - id: replace-style-id
    content: 将浅白样式ID从 1992174833714153216 替换为 1992216939216384896
    status: completed
---

将「浅白」地图样式的自定义样式 ID 从 `1992174833714153216` 更换为 `1992216939216384896`。仅需修改一处字符串值，无其他功能影响。

## Tech Stack

- 华为 Map Kit（`@kit.MapKit`）
- HarmonyOS ArkTS

## Implementation Approach

替换 `entry/src/main/ets/pages/Index.ets` 中第 868 行的字符串字面量，将浅白样式 ID 更新为新的值。这是单点文本替换，不涉及逻辑变更。

## Implementation Notes

- 修改前确认旧 ID 的引号包裹格式为单引号，新 ID 需保持相同的单引号格式。
- 修改后运行 `read_lints` 检查无语法错误即可。

## Directory Structure

```
entry/src/main/ets/pages/Index.ets  # [MODIFY] 第868行替换styleId字符串
```