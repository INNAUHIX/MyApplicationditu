export default {
  get(_url: string, _config?: object): Promise<object> {
    return Promise.resolve({});
  },
  post(_url: string, _data?: object, _config?: object): Promise<object> {
    return Promise.resolve({});
  }
};
